# Ignore everything in this directory by default (root .gitignore does this too),
# but allow the README and this .gitignore through so the directory documents itself.
*
!.gitignore
!README.md
!init.sh
!issue-client.sh
!test-scripts.sh
